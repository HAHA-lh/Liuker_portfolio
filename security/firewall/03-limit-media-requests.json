{
  "action": "rules.insert",
  "value": {
    "name": "LIUKER - limit abnormal media bursts",
    "description": "600 media requests per IP per 60-second regional window, preserving ordinary playback and seeking; excess returns 429.",
    "active": true,
    "conditionGroup": [{ "conditions": [
      { "type": "path", "op": "pre", "value": "/media/" }
    ] }],
    "action": { "mitigate": {
      "action": "rate_limit",
      "rateLimit": { "algo": "fixed_window", "window": 60, "limit": 600, "keys": ["ip"], "action": "rate_limit" }
    } }
  }
}
